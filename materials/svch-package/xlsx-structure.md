# Структура боевого xlsx — фиксация из сжатой памяти

**Файл:** `ГИС_ЦТИ__модель_оператора__налоги__дэшборд_.xlsx` (66.8 KB).
Приложен G в начале сессии 2026-05-09. Разобран через openpyxl в том же турне,
результат просочился в сжатую сводку после компактирования, но в коммиты не попал. Фиксирую
то что осталось. Полные формулы — переснять в новой сессии, когда G приложит файл
повторно и будет доступный `view` / `bash_tool`.

## 9 листов

### 1. `О_модели`
Титульный / поясняющий лист. Детали в памяти не сохранились.

### 2. `DASHBOARD`
4 рычага:
- **H** = 240 ч (часы/мес участника)
- **R** = 550 ₽/ч (ставка)
- **Регион** = 2 (какой-то выбор из списка)
- **Stress-test ОПС** = 0 (выключен по умолчанию)

Контингент (колонка плана): Y3 = 250 000, Y7 = 1 000 000 участников.

### 3. `PARAMS`
Каталог констант модели. Сохранилась одна ячейка:
- `PARAMS!B23` = 90 000 ₽ — CAPEX на привоз 1 уч. ⚠️ НЕСОСТЫК С КАНОНОМ (HANDOFF: 120 000 ₽).
  При аддендуме — поднять.

### 4. `Unit_Economics`
4 сценария. Разладка по сценариям в памяти не сохранилась.

### 5. `Escrow_Balance`
Накопление эскроу: 36 месяцев × 20% ФОТ; депозит-ставка 14%.

### 6. `TCO_Comparison`
Сравнение vs штатник 135 000 ₽/мес + sticky 17%.

### 7. `Fiscal_7Y`
3-уровневый фискальный каскад на 7 лет. Детали уровней в памяти не сохранились.

### 8. `Sensitivities`
Чувствительность по ключевым входам. Детали в памяти не сохранились.

### 9. `Operator_PnL_7Y`
P&L оператора на 7 лет:
- HQ: 0,3 → 3,0 млрд ₽/год (фикс по годам)
- Маркетинг: 300 → 100 млн ₽/год (убывающий хвост)
- Стартовые CAPEX: 300 млн ₽ × 3 года

## Дыры к открытию в новой сессии

- Формулы листа `Unit_Economics` (4 сценария) — выписать все 4.
- Структура каскада `Fiscal_7Y` (какие 3 уровня — вероятно инсвзносы/НДФЛ/НДС+НП, но надо сверить).
- Переменные `Sensitivities` (какие входы рвать).
- Связь `DASHBOARD` → `Unit_Economics` → `Operator_PnL_7Y` — как 4 рычага передаются в P&L.

## Команда для новой сессии

```python
import openpyxl
wb = openpyxl.load_workbook('/mnt/user-data/uploads/ГИС_ЦТИ__модель_оператора__налоги__дэшборд_.xlsx', data_only=False)
for sheet_name in wb.sheetnames:
    ws = wb[sheet_name]
    print(f'=== {sheet_name} ({ws.max_row}×{ws.max_column}) ===')
    for row in ws.iter_rows(values_only=False):
        for cell in row:
            if cell.value is not None:
                print(f'  {cell.coordinate}: {cell.value}')
```

Это выведёт все ячейки с формулами по всем 9 листам. Дальше — сопоставить
с этой фиксацией и дописать файл полными формулами.
